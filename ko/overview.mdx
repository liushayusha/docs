---
title: 'AIReiter — AI 이미지 및 영상 생성 API 플랫폼'
description: '통합 AI 이미지 및 영상 생성 API 플랫폼입니다. GPT-4o Image, Gemini, Sora2, VEO3 등 다양한 주류 모델을 지원합니다. 비동기 작업 처리, 투명한 가격 정책, 간단하고 사용하기 쉽습니다.'
---
import { apiKeyUrl } from '/snippets/_variables.mdx';

AIReiter는 이미지와 비디오 생성 서비스를 제공하는 통합 AI 콘텐츠 생성 API 플랫폼입니다. 간단한 REST API 호출을 통해 GPT-4o, Gemini, Sora2, VEO3 등 다양한 주류 AI 모델을 사용하여 고품질 이미지와 비디오 콘텐츠를 생성할 수 있습니다.

## 빠른 시작

<CardGroup cols={2}>
  <Card title="이미지 생성" icon="image" href="/ko/api-reference/images/gpt-4o/generation">
    GPT-4o, Gemini, Seedream 등의 모델을 사용하여 이미지를 생성합니다
  </Card>
  <Card title="비디오 생성" icon="video" href="/ko/api-reference/videos/sora2/generation">
    Sora2, VEO3 등의 모델을 사용하여 비디오 콘텐츠를 생성합니다
  </Card>
  <Card title="작업 상태 조회" icon="list-check" href="/ko/api-reference/tasks/status">
    비동기 작업의 실행 상태와 결과를 조회합니다
  </Card>
  <Card title="API Key 받기" icon="key" href={apiKeyUrl}>
    콘솔에 접속하여 API Key를 받으세요
  </Card>
</CardGroup>

## API 작업 흐름

### 1. 생성 작업 제출

모든 이미지 및 비디오 생성은 비동기 처리 모드를 사용합니다. 작업을 제출할 때 `out_task_id`(발신자 작업 ID)를 전달해야 하며, 시스템은 `out_task_id`를 반환합니다:

```bash
curl --request POST \
  --url https://aireiter.com/api/openapi/submit \
  --header 'Authorization: Bearer YOUR_API_KEY' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "gpt_4o",
    "params": {
      "prompt": "한 귀여운 고양이가 정원에 있습니다",
      "aspect_ratio": "1:1"
    },
    "out_task_id": "my_task_123456"
  }'
```

### 2. 작업 상태 조회

제출한 `out_task_id`를 사용하여 작업 진행 상황과 결과를 조회하세요:

```bash
curl --request POST \
  --url https://aireiter.com/api/openapi/query \
  --header 'Authorization: Bearer YOUR_API_KEY' \
  --header 'Content-Type: application/json' \
  --data-raw '{
    "out_task_id": "my_task_123456"
  }'
```

### 3. 생성 결과 가져오기

작업이 완료되면 응답에 생성된 이미지 또는 비디오 URL이 포함됩니다:

```json
{
  "statusCode": 200,
  "message": "",
  "data": {
    "out_task_id": "my_task_123456",
    "status": "completed",
    "output": [
      {
        "url": "https://s1.pxz.ai/upload/image-generator/xxx.jpeg"
      }
    ]
  }
}
```

## 지원하는 모델

### 이미지 생성 시리즈

AIReiter는 텍스트 기반 이미지 생성과 이미지 변환 기능을 지원하는 다양한 이미지 생성 모델을 제공합니다:

<CardGroup cols={2}>
  <Card title="GPT-4o Image" icon="image" href="/zh/api-reference/images/gpt-4o/generation">
    OpenAI의 고품질 이미지 생성 모델
  </Card>
  <Card title="Gemini-2.5-Flash (Nano banana)" icon="image" href="/zh/api-reference/images/nano-banana/generation">
    구글 최신 고속 이미지 생성 모델
  </Card>
  <Card title="Gemini-3-Pro (Nano banana pro)" icon="image" href="/zh/api-reference/images/gemini-3-pro/generation">
    구글 전문가급 이미지 생성 모델
  </Card>
  <Card title="Nano Banana V2" icon="image" href="/zh/api-reference/images/nano-banana-v2/generation">
    텍스트-이미지 변환, 이미지 변환, 이미지 편집 등 다양한 생성 모드 지원
  </Card>
  <Card title="Seedream-4.0" icon="image" href="/zh/api-reference/images/seedream-4/generation">
    효율적인 이미지 생성 솔루션
  </Card>
  <Card title="Seedream-4.5" icon="image" href="/zh/api-reference/images/seedream-4.5/generation">
    Seedream 4.5 버전
  </Card>
  <Card title="Seedream V5 lite" icon="image" href="/zh/api-reference/images/seedream-v5-lite/generation">
    Seedream 최신 버전, 온라인 검색 강화 생성 지원
  </Card>
</CardGroup>

### 비디오 생성 시리즈

텍스트 기반 비디오 생성 및 이미지 기반 비디오 생성을 지원하며, 다양한 해상도와 길이 옵션을 제공합니다:

<CardGroup cols={2}>
  <Card title="Sora2" icon="video" href="/ko/api-reference/videos/sora2/generation">
    OpenAI Sora2 비디오 생성 모델 (10-15초)
  </Card>
  <Card title="Sora2 Pro" icon="video" href="/zh/api-reference/videos/sora2-pro/generation">
    Sora2 전문 버전, 고화질 해상도 지원
  </Card>
  <Card title="VEO3.1" icon="video" href="/zh/api-reference/videos/veo3-1/generation">
    구글 VEO3.1 비디오 생성 모델
  </Card>
  <Card title="VEO3.1 Fast" icon="video" href="/zh/api-reference/videos/veo3-1-fast/generation">
    VEO3.1 빠른 버전
  </Card>
  <Card title="Seedance-1.5-pro" icon="video" href="/zh/api-reference/videos/seedance-1-5-pro/generation">
    시작 및 끝 프레임 제어와 오디오 생성 지원 (4-12초)
  </Card>
  <Card title="Seedance Pro Fast" icon="video" href="/zh/api-reference/videos/seedance_pro_fast/generation">
    Seedance 빠른 버전, 시작 프레임 제어 지원
  </Card>
  <Card title="Seedance Pro" icon="video" href="/zh/api-reference/videos/seedance_pro/generation">
    Seedance 전문 버전, 시작 및 끝 프레임 제어 지원
  </Card>
</CardGroup>

## 핵심 기능

### 비동기 작업 처리

모든 생성 요청은 비동기 처리 모드를 사용하여 장시간 대기를 방지합니다:

- 작업 제출 시 `out_task_id`(발신자 작업 ID, 비즈니스 식별용)를 전달해야 합니다
- 제출한 `out_task_id`를 즉시 반환합니다
- `out_task_id`를 사용하여 작업 상태를 조회할 수 있습니다
- 웹훅 콜백 알림 지원(선택 사항)
- 작업 상태 실시간 업데이트: `pending` → `processing` → `completed`

### 다중 모델 지원

통합 API 엔드포인트로 다양한 이미지 및 영상 생성 모델 지원:

- **이미지 생성**: GPT-4o, Gemini, Seedream 등 5개 모델
- **영상 생성**: Sora2, VEO3, Seedance 등 7개 모델
- 통일된 요청 형식 및 응답 구조
- 유연한 파라미터 설정

### 투명한 가격 정책

포인트 기반 과금으로 투명하고 명확함:

- 각 작업별 예상 소모 포인트 반환
- 작업 완료 후 실제 소모 포인트 표시
- 성공적으로 생성된 콘텐츠에만 비용 지불
- 숨겨진 비용 없음

### 유연한 설정

다양한 생성 파라미터 설정 지원:

- **이미지**: 다양한 가로 세로 비율(1:1, 3:2, 2:3)
- **영상**: 다양한 가로 세로 비율(16:9, 9:16), 길이(10초, 15초), 해상도
- 참조 이미지 지원(이미지에서 이미지, 이미지에서 영상)

## 인증 방법

모든 API 요청은 Bearer Token을 사용하여 인증해야 합니다:

```
Authorization: Bearer YOUR_API_KEY
```

**API Key 얻기:**

<a href={apiKeyUrl} target="_blank">API Key 관리 페이지</a>에 접속하여 API Key를 얻으세요.

## 자주 묻는 질문

<AccordionGroup>
  <Accordion title="API 키는 어떻게 얻나요?">
    <a href={apiKeyUrl} target="_blank">API 키 관리 페이지</a>에 접속하여 로그인하면 API 키를 생성하고 관리할 수 있습니다.
  </Accordion>

  <Accordion title="이미지와 비디오 생성에는 얼마나 걸리나요?">
    모든 생성 작업은 비동기로 처리됩니다. 이미지 생성은 보통 몇 초에서 수십 초 걸리며, 비디오 생성은 길이와 해상도에 따라 보통 1~5분 소요됩니다. 작업 제출 후 즉시 out_task_id가 반환되며, 상태 조회 API를 통해 진행 상황을 추적할 수 있습니다.
  </Accordion>

  <Accordion title="작업 상태는 어떻게 조회하나요?">
    작업 상태 조회 API를 사용하여 설정한 `out_task_id`를 전달하세요:

    ```bash
    POST https://aireiter.com/api/openapi/query
    ```

    자세한 내용은 [작업 상태 조회 문서](/zh/api-reference/tasks/status)를 참조하세요.
  </Accordion>

  <Accordion title="out_task_id란 무엇인가요?">
    `out_task_id`는 작업을 요청하는 쪽에서 설정하는 작업 ID입니다:

    - **필수 파라미터**: 작업 제출 시 반드시 `out_task_id`를 전달해야 합니다
    - **업무 식별**: 시스템 내 주문 번호, 업무 ID 등으로 식별 가능
    - **유연한 조회**: `out_task_id`로 직접 작업 상태 조회 가능
    - **결과 연동**: 조회 결과에 `out_task_id`가 포함되어 업무 데이터와 연동하기 용이함
  </Accordion>

  <Accordion title="요금은 어떻게 청구되나요?">
    AIReiter는 포인트제로 청구됩니다:

    - 작업 제출 시 예상 소모 포인트(`estimated_credits`) 반환
    - 작업 완료 후 실제 소모 포인트(`credits_used`) 반환
    - 성공적으로 생성된 내용에 대해서만 요금 청구, 실패한 작업은 청구되지 않음
    - 모델에 따라 포인트 소모량 상이
  </Accordion>

  <Accordion title="지원하는 이미지 및 비디오 형식은 무엇인가요?">
    - **이미지**: JPEG, PNG 등 일반적인 형식 지원
    - **비디오**: MP4 형식 출력
    - **참고 이미지**: 공개 접근 가능한 이미지 URL 입력 지원
  </Accordion>
</AccordionGroup>

## 다음 단계

<CardGroup cols={2}>
  <Card title="이미지 생성 문서 보기" icon="image" href="/ko/api-reference/images/gpt-4o/generation">
    다양한 이미지 생성 모델 사용하는 방법을 알아보세요
  </Card>
  <Card title="비디오 생성 문서 보기" icon="video" href="/ko/api-reference/videos/sora2/generation">
    비디오 콘텐츠 생성 방법을 알아보세요
  </Card>
  <Card title="작업 관리" icon="list-check" href="/ko/api-reference/tasks/status">
    작업 상태를 관리하고 조회하는 방법을 배우세요
  </Card>
  <Card title="API Key 받기" icon="key" href={apiKeyUrl}>
    AIReiter를 즉시 시작하세요
  </Card>
</CardGroup>