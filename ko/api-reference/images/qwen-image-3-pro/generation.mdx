---
title: 'Qwen Image 3.0 Pro 이미지 생성 및 편집'
description: |
    - 포스터, 텍스트 레이아웃, 메뉴, 인포그래픽 및 고디테일 이미지 편집에 적합
    - 비동기 처리 모드로, 후속 조회를 위한 작업 ID를 반환합니다
    - 텍스트-이미지 생성, 최대 3장의 참고 이미지, 한 번에 1-6장의 이미지 생성을 지원합니다
    - 1K / 2K 해상도와 7가지 일반적인 이미지 비율을 지원합니다
    - 제출된 prompt는 플랫폼 민감어 / 안전 검사를 거치며, 위반 콘텐츠는 직접 거절됩니다
api: 'POST https://aireiter.com/api/openapi/submit'
---
import { apiKeyUrl } from '/snippets/_variables.mdx';

## Authorizations

<ParamField header="Authorization" type="string" required>
  모든 인터페이스는 Bearer Token을 사용하여 인증해야 합니다.

  <a href={apiKeyUrl} target="_blank">API Key 관리 페이지</a>에 접속하여 API Key를 가져오고, 요청 헤더에 다음을 추가하세요:

  ```text
  Authorization: Bearer YOUR_API_KEY
  ```
</ParamField>

## Body

<ParamField body="model" type="string" required>
  Model name, fixed value: `"qwen_image_3_pro"`.
</ParamField>

<ParamField body="params" type="object" required>
  Model parameters object.

  <Expandable title="Properties of the params object">
    <ParamField body="prompt" type="string" required>
      Image generation or editing instruction. Supports Chinese and English.

      The Pro model is suitable for tasks that require readable text, complex hierarchy, dense layouts, and high-detail rendering. It is recommended to clearly describe the text content, font style, hierarchy, position, whitespace, and the reference image features that need to be preserved.
    </ParamField>

    <ParamField body="image_url" type="string[]">
      Array of reference images. When provided, image-to-image mode is used; when omitted, text-to-image mode is used.

      * Up to 3 reference images
      * Maximum size of a single image is 10MB
      * Supports jpeg, png, webp
      * Supports publicly accessible URLs or complete Base64 Data URIs

      <Warning>
        Image URLs must be directly accessible from the public internet and cannot rely on login status, cookies, or temporary page authorization.
      </Warning>
    </ParamField>

    <ParamField body="resolution" type="string" default="1K">
      Output resolution tier.

      * `1K` - Base resolution (default)
      * `2K` - High-definition resolution, suitable for text, posters, and dense layouts
    </ParamField>

    <ParamField body="aspect_ratio" type="string" default="1:1">
      Output image aspect ratio.

      Supported values:

      * `1:1` - Square (default)
      * `3:2` / `2:3` - Classic landscape / portrait
      * `4:3` / `3:4` - Standard landscape / portrait
      * `16:9` / `9:16` - Widescreen landscape / portrait

    </ParamField>

    <ParamField body="output_n" type="integer" default="1">
      Number of images generated for this task, ranging from `1` to `6`.
    </ParamField>

    <ParamField body="prompt_extend" type="boolean" default="false">
      Smart prompt rewriting. Disabled by default to keep results predictable. Set to `true` when smart rewriting is needed.
    </ParamField>

    <ParamField body="prompt_extend_mode" type="string" default="direct">
      Prompt rewriting mode. Effective only when `prompt_extend` is `true`.

      * `direct` - Balanced rewriting, supports both text-to-image and image-to-image
      * `agent` - Aggressive rewriting, text-to-image only
    </ParamField>

    <ParamField body="negative_prompt" type="string">
      Negative prompt, used to describe content you do not want to appear in the result, such as blur, garbled text, crowded layout, or structural distortion.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="out_task_id" type="string" required>
  Caller-defined task ID, used for idempotent submission and result queries.

  * Length: 1-64 characters
  * It is recommended to use only letters, numbers, underscores, and hyphens
  * Do not reuse the same ID under the same API Key
</ParamField>

## Response

<ResponseField name="out_task_id" type="string">
  호출자 작업 ID로, 작업 상태와 결과를 조회하는 데 사용할 수 있습니다.
</ResponseField>

<ResponseField name="status" type="string">
  초기 상태로, 일반적으로 `"pending"`입니다.
</ResponseField>

<ResponseField name="created_at" type="string">
  작업 생성 시간, ISO 8601 형식입니다.
</ResponseField>

## 검색 결과

제출이 성공한 후, 동일한 `out_task_id`를 사용하여 조회 API를 호출합니다:

```bash
curl --request POST \
  --url https://aireiter.com/api/openapi/query \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "out_task_id": "qwen_image_3_pro_task_001"
  }'
```

작업 상태에는 `pending`, `processing`, `completed`, `failed`가 포함됩니다. 작업이 완료되면 이미지 배열은 `data.output`에 있습니다.

<RequestExample>
  ```bash cURL（文生图）
  curl --request POST \
    --url https://aireiter.com/api/openapi/submit \
    --header 'Authorization: Bearer <token>' \
    --header 'Content-Type: application/json' \
    --data '{
      "model": "qwen_image_3_pro",
      "params": {
        "prompt": "프리미엄 커피숍을 위한 세로형 메뉴 포스터를 디자인해 주세요. 제목은 MORNING ROAST로 하고, 가격 단계는 읽기 쉽게 표시하며, 현대적인 편집 디자인과 사실적인 커피 사진 스타일로 표현해 주세요",
        "resolution": "2K",
        "aspect_ratio": "3:4",
        "output_n": 1,
        "prompt_extend": true,
        "prompt_extend_mode": "agent",
        "negative_prompt": "글자 깨짐, 흐림, 답답한 레이아웃, 워터마크"
      },
      "out_task_id": "qwen_image_3_pro_task_001"
    }'
  ```

  ```bash cURL（多参考图编辑）
  curl --request POST \
    --url https://aireiter.com/api/openapi/submit \
    --header 'Authorization: Bearer <token>' \
    --header 'Content-Type: application/json' \
    --data '{
      "model": "qwen_image_3_pro",
      "params": {
        "prompt": "첫 번째 이미지의 상품 본체, 두 번째 이미지의 색감, 세 번째 이미지의 레이아웃을 사용하여 고급스러운 가로형 광고를 생성하고, 라벨 텍스트는 선명하게 유지해 주세요",
        "image_url": [
          "https://example.com/product.png",
          "https://example.com/color-reference.png",
          "https://example.com/layout-reference.png"
        ],
        "resolution": "2K",
        "aspect_ratio": "16:9",
        "output_n": 1
      },
      "out_task_id": "qwen_image_3_pro_edit_001"
    }'
  ```

  ```python Python
  import requests

  response = requests.post(
      "https://aireiter.com/api/openapi/submit",
      headers={
          "Authorization": "Bearer <token>",
          "Content-Type": "application/json"
      },
      json={
          "model": "qwen_image_3_pro",
          "params": {
              "prompt": "현대적인 테크 제품 출시 포스터, 제목은 선명하게, 정보 계층은 명확하게, 어두운 배경",
              "resolution": "2K",
              "aspect_ratio": "16:9",
              "output_n": 1
          },
          "out_task_id": "qwen_image_3_pro_python_001"
      }
  )

  print(response.json())
  ```

  ```javascript JavaScript
  const response = await fetch("https://aireiter.com/api/openapi/submit", {
    method: "POST",
    headers: {
      Authorization: "Bearer <token>",
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      model: "qwen_image_3_pro",
      params: {
        prompt: "현대적인 테크 제품 출시 포스터, 제목은 선명하게, 정보 계층은 명확하게, 어두운 배경",
        resolution: "2K",
        aspect_ratio: "16:9",
        output_n: 1
      },
      out_task_id: "qwen_image_3_pro_javascript_001"
    })
  });

  console.log(await response.json());
  ```
</RequestExample>