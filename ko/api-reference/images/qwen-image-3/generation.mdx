---
title: 'Qwen Image 3.0 이미지 생성 및 편집'
description: |
    - 비동기 처리 모드이며, 후속 조회를 위한 작업 ID를 반환합니다
    - 텍스트-이미지 생성과 이미지-이미지 생성은 지원되며, 참고 이미지를 전달하지 않으면 텍스트-이미지 생성으로 처리됩니다
    - 최대 3장의 참고 이미지와 한 번에 1-6장의 이미지 생성을 지원합니다
    - 1K / 2K 해상도와 7가지 일반적인 이미지 비율을 지원합니다
    - 제출된 prompt는 플랫폼의 민감어 / 안전 검사를 거치며, 위반 콘텐츠는 직접 거부됩니다
api: 'POST https://aireiter.com/api/openapi/submit'
---
import { apiKeyUrl } from '/snippets/_variables.mdx';

## Authorizations

<ParamField header="Authorization" type="string" required>
  모든 API는 인증을 위해 Bearer Token을 사용해야 합니다.

  <a href={apiKeyUrl} target="_blank">API Key 관리 페이지</a>에 접속하여 API Key를 가져온 후, 요청 헤더에 다음을 추가하세요:

  ```text
  Authorization: Bearer YOUR_API_KEY
  ```
</ParamField>

## Body

<ParamField body="model" type="string" required>
  Model name, fixed value: `"qwen_image_3"`.
</ParamField>

<ParamField body="params" type="object" required>
  Model parameter object.

  <Expandable title="params Object Properties">
    <ParamField body="prompt" type="string" required>
      Image generation or editing instructions. Supports Chinese and English.

      It is recommended to clearly describe the subject, scene, composition, lighting, materials, text content, and the parts in the reference image that need to be kept or modified.
    </ParamField>

    <ParamField body="image_url" type="string[]">
      Reference image array. When passed in, image-to-image mode is used; when omitted, text-to-image mode is used.

      * Up to 3 reference images
      * Maximum 10MB per image
      * Supports jpeg, png, webp
      * Supports publicly accessible URLs or complete Base64 Data URIs

      <Warning>
        The image URL must be directly accessible from the public internet and cannot rely on login status, cookies, or temporary page authorization.
      </Warning>
    </ParamField>

    <ParamField body="resolution" type="string" default="1K">
      Output resolution tier.

      * `1K` - Basic resolution (default)
      * `2K` - High-definition resolution
    </ParamField>

    <ParamField body="aspect_ratio" type="string" default="1:1">
      Output image aspect ratio.

      Supported values:

      * `1:1` - Square (default)
      * `3:2` / `2:3` - Classic landscape / portrait
      * `4:3` / `3:4` - Standard landscape / portrait
      * `16:9` / `9:16` - Widescreen landscape / portrait

    </ParamField>

    <ParamField body="output_n" type="integer" default="1">
      Number of images generated by this task, ranging from `1` to `6`.
    </ParamField>

    <ParamField body="prompt_extend" type="boolean" default="false">
      Intelligent prompt rewriting. Disabled by default to keep results predictable. Set to `true` when intelligent rewriting is needed.
    </ParamField>

    <ParamField body="prompt_extend_mode" type="string" default="direct">
      Prompt rewriting mode. Takes effect only when `prompt_extend` is `true`.

      * `direct` - Balanced rewriting, supports both text-to-image and image-to-image
      * `agent` - Aggressive rewriting, supports text-to-image only
    </ParamField>

    <ParamField body="negative_prompt" type="string">
      Negative prompt, used to describe content that you do not want to appear in the result, such as blur, distortion, or incorrect text.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="out_task_id" type="string" required>
  Caller-defined task ID, used for idempotent submission and result querying.

  * Length is 1-64 characters
  * It is recommended to use only letters, numbers, underscores, and hyphens
  * Do not reuse the same one under the same API Key
</ParamField>

## Response

<ResponseField name="out_task_id" type="string">
  호출자 작업 ID로, 작업 상태와 결과를 조회하는 데 사용할 수 있습니다.
</ResponseField>

<ResponseField name="status" type="string">
  초기 상태이며, 일반적으로 `"pending"`입니다.
</ResponseField>

<ResponseField name="created_at" type="string">
  작업 생성 시간, ISO 8601 형식입니다.
</ResponseField>

## Query Results

After the submission succeeds, call the query endpoint using the same `out_task_id`:

```bash
curl --request POST \
  --url https://aireiter.com/api/openapi/query \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "out_task_id": "qwen_image_3_task_001"
  }'
```

Task statuses include `pending`, `processing`, `completed`, and `failed`. After the task is completed, the image array is located in `data.output`.

<RequestExample>
  ```bash cURL（Text-to-Image）
  curl --request POST \
    --url https://aireiter.com/api/openapi/submit \
    --header 'Authorization: Bearer <token>' \
    --header 'Content-Type: application/json' \
    --data '{
      "model": "qwen_image_3",
      "params": {
        "prompt": "A futuristic electric sports car in front of a modern glass building, blue hour, commercial automotive photography, realistic materials and natural shadows, no text, no watermark",
        "resolution": "2K",
        "aspect_ratio": "16:9",
        "output_n": 1,
        "prompt_extend": true,
        "prompt_extend_mode": "agent"
      },
      "out_task_id": "qwen_image_3_task_001"
    }'
  ```

  ```bash cURL（Image-to-Image）
  curl --request POST \
    --url https://aireiter.com/api/openapi/submit \
    --header 'Authorization: Bearer <token>' \
    --header 'Content-Type: application/json' \
    --data '{
      "model": "qwen_image_3",
      "params": {
        "prompt": "Keep the product subject and label structure, change the background to a dark gray premium advertising studio, and add cool rim lighting",
        "image_url": ["https://example.com/product-reference.png"],
        "resolution": "1K",
        "aspect_ratio": "1:1",
        "output_n": 1
      },
      "out_task_id": "qwen_image_3_edit_001"
    }'
  ```

  ```python Python
  import requests

  response = requests.post(
      "https://aireiter.com/api/openapi/submit",
      headers={
          "Authorization": "Bearer <token>",
          "Content-Type": "application/json"
      },
      json={
          "model": "qwen_image_3",
          "params": {
              "prompt": "Cinematic product advertising photography, a glass perfume bottle placed on a black stone pedestal",
              "resolution": "1K",
              "aspect_ratio": "16:9",
              "output_n": 1
          },
          "out_task_id": "qwen_image_3_python_001"
      }
  )

  print(response.json())
  ```

  ```javascript JavaScript
  const response = await fetch("https://aireiter.com/api/openapi/submit", {
    method: "POST",
    headers: {
      Authorization: "Bearer <token>",
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      model: "qwen_image_3",
      params: {
        prompt: "Cinematic product advertising photography, a glass perfume bottle placed on a black stone pedestal",
        resolution: "1K",
        aspect_ratio: "16:9",
        output_n: 1
      },
      out_task_id: "qwen_image_3_javascript_001"
    })
  });

  console.log(await response.json());
  ```
</RequestExample>