---
title: 'Grok Imagine Image 2.0 画像生成と編集'
description: |
    - 非同期画像生成。送信後、タスク照会 API で結果を取得します
    - テキストから画像、画像から画像、画像編集をサポート
    - 1K / 2K、Low / Medium 品質、最大 10 枚の出力をサポート
    - 最大 3 枚の参考画像と 14 種類のアスペクト比オプションをサポート
    - 送信された prompt はプラットフォームのセンシティブワード / 安全審査を通過し、違反コンテンツは直接拒否されます
api: 'POST https://aireiter.com/api/openapi/submit'
---
import { apiKeyUrl } from '/snippets/_variables.mdx';

## Authorizations

<ParamField header="Authorization" type="string" required>
  すべてのエンドポイントでは Bearer Token 認証が必要です。

  <a href={apiKeyUrl} target="_blank">API Key 管理ページ</a> にアクセスして API Key を取得し、Request Header に追加してください:

  ```text
  Authorization: Bearer YOUR_API_KEY
  ```
</ParamField>

## Body

<ParamField body="model" type="string" required>
  モデル名、固定値：`"grok_imagine_image_2_0"`。
</ParamField>

<ParamField body="params" type="object" required>
  モデルパラメーターオブジェクト。

  <Expandable title="params オブジェクトの Properties">
    <ParamField body="prompt" type="string" required>
      画像生成または編集の指示。中国語と英語をサポートしています。

      複数の参考画像を使用する場合は、プロンプト内で `<IMAGE_0>`、`<IMAGE_1>`、`<IMAGE_2>` を使って対応する画像を参照できます。
    </ParamField>

    <ParamField body="image_url" type="string[]">
      参考画像の配列。省略した場合はテキストから画像を生成します。渡すと自動的に画像から画像への生成または画像編集モードが使用されます。

      * 最大 3枚
      * 1枚あたり最大 10MB
      * JPG、PNG、WebP をサポート
      * 公開 URL、完全な Base64 Data URI、または純粋な Base64 をサポート

      <Warning>
        URL を使用する場合、画像はインターネットから直接アクセスできる必要があり、ログイン状態、Cookie、または一時的なページ認可に依存できません。
      </Warning>
    </ParamField>

    <ParamField body="aspect_ratio" type="string" default="auto">
      出力画像のアスペクト比。サポート：

      * `auto`（デフォルト）
      * `1:1`
      * `4:3` / `3:4`
      * `3:2` / `2:3`
      * `16:9` / `9:16`
      * `2:1` / `1:2`
      * `19.5:9` / `9:19.5`
      * `20:9` / `9:20`

      カスタム `WIDTHxHEIGHT` ピクセルサイズはサポートされていません。
    </ParamField>

    <ParamField body="resolution" type="string" default="1K">
      出力解像度のレベル：`1K`（デフォルト）または `2K`。`4K` はサポートされていません。
    </ParamField>

    <ParamField body="quality" type="string" default="medium">
      生成品質のレベル：

      * `low` - より高速で、費用が低い
      * `medium` - より高いディテール（デフォルト）
    </ParamField>

    <ParamField body="output_n" type="integer" default="1">
      出力画像の枚数。値の範囲は `1` から `10` です。各画像は個別に課金されます。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="out_task_id" type="string" required>
  呼び出し元が指定するタスク ID。冪等な送信と結果の照会に使用します。

  * 長さは 1-64文字
  * 英字、数字、アンダースコア、ハイフンのみサポート
  * 同じ API Key では重複使用できません
</ParamField>

## 検索結果

送信成功後、[タスク状態の取得](/en/api-reference/tasks/status) を呼び出し、同じ `out_task_id` を使用して検索します。タスクが完了すると、画像結果は `data.output` にあります。

<RequestExample>
  ```bash cURL（画像生成）
  curl --request POST \
    --url https://aireiter.com/api/openapi/submit \
    --header 'Authorization: Bearer <token>' \
    --header 'Content-Type: application/json' \
    --data '{
      "model": "grok_imagine_image_2_0",
      "params": {
        "prompt": "雨の夜の東京の街角、ネオンが濡れた路面に映り込む、シネマティックな写真",
        "aspect_ratio": "16:9",
        "resolution": "2K",
        "quality": "medium",
        "output_n": 1
      },
      "out_task_id": "grok_image_2_text_001"
    }'
  ```

  ```bash cURL（画像編集）
  curl --request POST \
    --url https://aireiter.com/api/openapi/submit \
    --header 'Authorization: Bearer <token>' \
    --header 'Content-Type: application/json' \
    --data '{
      "model": "grok_imagine_image_2_0",
      "params": {
        "prompt": "<IMAGE_0> の人物を <IMAGE_1> のシーンに入れ、光を一致させる",
        "image_url": [
          "https://example.com/person.png",
          "https://example.com/scene.png"
        ],
        "aspect_ratio": "16:9",
        "resolution": "1K",
        "quality": "medium",
        "output_n": 1
      },
      "out_task_id": "grok_image_2_edit_001"
    }'
  ```

  ```javascript JavaScript
  const response = await fetch("https://aireiter.com/api/openapi/submit", {
    method: "POST",
    headers: {
      Authorization: "Bearer <token>",
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      model: "grok_imagine_image_2_0",
      params: {
        prompt: "ミニマルな白いフォトスタジオにある青い陶器のカップ、柔らかな商品用ライティング",
        aspect_ratio: "1:1",
        resolution: "1K",
        quality: "low",
        output_n: 1
      },
      out_task_id: "grok_image_2_js_001"
    })
  });

  console.log(await response.json());
  ```
</RequestExample>

## クエリ結果

送信成功後、[タスクステータスを取得](/en/api-reference/tasks/status) を呼び出し、同じ `out_task_id` を使ってクエリします。タスク完了後、画像結果は `data.output` にあります。

<RequestExample>
  ```bash cURL（テキストから画像生成）
  curl --request POST \
    --url https://aireiter.com/api/openapi/submit \
    --header 'Authorization: Bearer <token>' \
    --header 'Content-Type: application/json' \
    --data '{
      "model": "grok_imagine_image_2_0",
      "params": {
        "prompt": "雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影",
        "aspect_ratio": "16:9",
        "resolution": "2K",
        "quality": "medium",
        "output_n": 1
      },
      "out_task_id": "grok_image_2_text_001"
    }'
  ```

  ```bash cURL（画像編集）
  curl --request POST \
    --url https://aireiter.com/api/openapi/submit \
    --header 'Authorization: Bearer <token>' \
    --header 'Content-Type: application/json' \
    --data '{
      "model": "grok_imagine_image_2_0",
      "params": {
        "prompt": "<IMAGE_0> の人物を <IMAGE_1> のシーンに配置し、ライティングを統一してください",
        "image_url": [
          "https://example.com/person.png",
          "https://example.com/scene.png"
        ],
        "aspect_ratio": "16:9",
        "resolution": "1K",
        "quality": "medium",
        "output_n": 1
      },
      "out_task_id": "grok_image_2_edit_001"
    }'
  ```

  ```python Python
  import requests

  url = "https://aireiter.com/api/openapi/submit"

  payload = {
      "model": "grok_imagine_image_2_0",
      "params": {
          "prompt": "雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影",
          "aspect_ratio": "16:9",
          "resolution": "2K",
          "quality": "medium",
          "output_n": 1
      },
      "out_task_id": "grok_image_2_text_001"
  }

  headers = {
      "Authorization": "Bearer <token>",
      "Content-Type": "application/json"
  }

  response = requests.post(url, json=payload, headers=headers)

  print(response.json())
  ```

  ```javascript JavaScript
  const url = "https://aireiter.com/api/openapi/submit";

  const payload = {
    model: "grok_imagine_image_2_0",
    params: {
      prompt: "雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影",
      aspect_ratio: "16:9",
      resolution: "2K",
      quality: "medium",
      output_n: 1
    },
    out_task_id: "grok_image_2_text_001"
  };

  const headers = {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  };

  fetch(url, {
    method: "POST",
    headers: headers,
    body: JSON.stringify(payload)
  })
    .then(response => response.json())
    .then(data => console.log(data))
    .catch(error => console.error('Error:', error));
  ```

  ```go Go
  package main

  import (
      "bytes"
      "encoding/json"
      "fmt"
      "io/ioutil"
      "net/http"
  )

  func main() {
      url := "https://aireiter.com/api/openapi/submit"

      payload := map[string]interface{}{
          "model": "grok_imagine_image_2_0",
          "params": map[string]interface{}{
              "prompt": "雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影",
              "aspect_ratio": "16:9",
              "resolution": "2K",
              "quality": "medium",
              "output_n": 1,
          },
          "out_task_id": "grok_image_2_text_001",
      }

      jsonData, _ := json.Marshal(payload)

      req, _ := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
      req.Header.Set("Authorization", "Bearer <token>")
      req.Header.Set("Content-Type", "application/json")

      client := &http.Client{}
      resp, err := client.Do(req)
      if err != nil {
          panic(err)
      }
      defer resp.Body.Close()

      body, _ := ioutil.ReadAll(resp.Body)
      fmt.Println(string(body))
  }
  ```

  ```java Java
  import java.net.http.HttpClient;
  import java.net.http.HttpRequest;
  import java.net.http.HttpResponse;
  import java.net.URI;

  public class Main {
      public static void main(String[] args) throws Exception {
          String url = "https://aireiter.com/api/openapi/submit";

          String payload = """
              {
                "model": "grok_imagine_image_2_0",
                "params": {
                  "prompt": "雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影",
                  "aspect_ratio": "16:9",
                  "resolution": "2K",
                  "quality": "medium",
                  "output_n": 1
                },
                "out_task_id": "grok_image_2_text_001"
              }
          """;

          HttpClient client = HttpClient.newHttpClient();
          HttpRequest request = HttpRequest.newBuilder()
              .uri(URI.create(url))
              .header("Authorization", "Bearer <token>")
              .header("Content-Type", "application/json")
              .POST(HttpRequest.BodyPublishers.ofString(payload))
              .build();

          HttpResponse<String> response = client.send(request,
              HttpResponse.BodyHandlers.ofString());

          System.out.println(response.body());
      }
  }
  ```

  ```php PHP
  <?php

  $url = "https://aireiter.com/api/openapi/submit";

  $payload = [
      "model" => "grok_imagine_image_2_0",
      "params" => [
          "prompt" => "雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影",
          "aspect_ratio" => "16:9",
          "resolution" => "2K",
          "quality" => "medium",
          "output_n" => 1
      ],
      "out_task_id" => "grok_image_2_text_001"
  ];

  $ch = curl_init($url);
  curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
  curl_setopt($ch, CURLOPT_POST, true);
  curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($payload));
  curl_setopt($ch, CURLOPT_HTTPHEADER, [
      "Authorization: Bearer <token>",
      "Content-Type: application/json"
  ]);

  $response = curl_exec($ch);
  curl_close($ch);

  echo $response;
  ?>
  ```

  ```ruby Ruby
  require 'net/http'
  require 'json'
  require 'uri'

  url = URI("https://aireiter.com/api/openapi/submit")

  payload = {
    model: "grok_imagine_image_2_0",
    params: {
      prompt: "雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影",
      aspect_ratio: "16:9",
      resolution: "2K",
      quality: "medium",
      output_n: 1
    },
    out_task_id: "grok_image_2_text_001"
  }

  http = Net::HTTP.new(url.host, url.port)

  request = Net::HTTP::Post.new(url)
  request["Authorization"] = "Bearer <token>"
  request["Content-Type"] = "application/json"
  request.body = payload.to_json

  response = http.request(request)
  puts response.body
  ```

  ```swift Swift
  import Foundation

  let url = URL(string: "https://aireiter.com/api/openapi/submit")!

  let payload: [String: Any] = [
      "model": "grok_imagine_image_2_0",
      "params": [
          "prompt": "雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影",
          "aspect_ratio": "16:9",
          "resolution": "2K",
          "quality": "medium",
          "output_n": 1
      ],
      "out_task_id": "grok_image_2_text_001"
  ]

  var request = URLRequest(url: url)
  request.httpMethod = "POST"
  request.setValue("Bearer <token>", forHTTPHeaderField: "Authorization")
  request.setValue("application/json", forHTTPHeaderField: "Content-Type")
  request.httpBody = try? JSONSerialization.data(withJSONObject: payload)

  let task = URLSession.shared.dataTask(with: request) { data, response, error in
      if let error = error {
          print("Error: \(error)")
          return
      }

      if let data = data, let responseString = String(data: data, encoding: .utf8) {
          print(responseString)
      }
  }

  task.resume()
  ```

  ```csharp C#
  using System;
  using System.Net.Http;
  using System.Text;
  using System.Threading.Tasks;

  class Program
  {
      static async Task Main(string[] args)
      {
          var url = "https://aireiter.com/api/openapi/submit";

          var payload = @"
                {
                    ""model"": ""grok_imagine_image_2_0"",
                    ""params"": {
                        ""prompt"": ""雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影"",
                        ""aspect_ratio"": ""16:9"",
                        ""resolution"": ""2K"",
                        ""quality"": ""medium"",
                        ""output_n"": 1
                    },
                    ""out_task_id"": ""grok_image_2_text_001""
                }";

          using var client = new HttpClient();
          client.DefaultRequestHeaders.Add("Authorization", "Bearer <token>");

          var content = new StringContent(payload, Encoding.UTF8, "application/json");
          var response = await client.PostAsync(url, content);
          var result = await response.Content.ReadAsStringAsync();

          Console.WriteLine(result);
      }
  }
  ```

  ```c c
  #include <stdio.h>
  #include <curl/curl.h>

  int main(void) {
      CURL *curl;
      CURLcode res;

      curl_global_init(CURL_GLOBAL_DEFAULT);
      curl = curl_easy_init();

      if(curl) {
          const char *url = "https://aireiter.com/api/openapi/submit";
          const char *payload = "{\"model\":\"grok_imagine_image_2_0\",\"params\":{\"prompt\":\"雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影\",\"aspect_ratio\":\"16:9\",\"resolution\":\"2K\",\"quality\":\"medium\",\"output_n\":1},\"out_task_id\":\"grok_image_2_text_001\"}";

          struct curl_slist *headers = NULL;
          headers = curl_slist_append(headers, "Authorization: Bearer <token>");
          headers = curl_slist_append(headers, "Content-Type: application/json");

          curl_easy_setopt(curl, CURLOPT_URL, url);
          curl_easy_setopt(curl, CURLOPT_POSTFIELDS, payload);
          curl_easy_setopt(curl, CURLOPT_HTTPHEADER, headers);

          res = curl_easy_perform(curl);

          if(res != CURLE_OK) {
              fprintf(stderr, "curl_easy_perform() failed: %s\n",
                      curl_easy_strerror(res));
          }

          curl_slist_free_all(headers);
          curl_easy_cleanup(curl);
      }

      curl_global_cleanup();
      return 0;
  }
  ```

  ```objectivec Objective-C
  #import <Foundation/Foundation.h>

  int main(int argc, const char * argv[]) {
      @autoreleasepool {
          NSURL *url = [NSURL URLWithString:@"https://aireiter.com/api/openapi/submit"];

          NSDictionary *payload = @{
              @"model": @"grok_imagine_image_2_0",
              @"params": @{
                  @"prompt": @"雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影",
                  @"aspect_ratio": @"16:9",
                  @"resolution": @"2K",
                  @"quality": @"medium",
                  @"output_n": @1
              },
              @"out_task_id": @"grok_image_2_text_001"
          };

          NSError *error;
          NSData *jsonData = [NSJSONSerialization dataWithJSONObject:payload
                                                            options:0
                                                              error:&error];

          NSMutableURLRequest *request = [NSMutableURLRequest requestWithURL:url];
          [request setHTTPMethod:@"POST"];
          [request setValue:@"Bearer <token>" forHTTPHeaderField:@"Authorization"];
          [request setValue:@"application/json" forHTTPHeaderField:@"Content-Type"];
          [request setHTTPBody:jsonData];

          NSURLSessionDataTask *task = [[NSURLSession sharedSession]
              dataTaskWithRequest:request
              completionHandler:^(NSData *data, NSURLResponse *response, NSError *error) {
                  if (error) {
                      NSLog(@"Error: %@", error);
                      return;
                  }
                  NSString *result = [[NSString alloc] initWithData:data
                                                          encoding:NSUTF8StringEncoding];
                  NSLog(@"%@", result);
              }];

          [task resume];
          [[NSRunLoop mainRunLoop] run];
      }
      return 0;
  }
  ```

  ```ocaml OCaml
  (* Requires cohttp and yojson libraries *)
  open Lwt
  open Cohttp
  open Cohttp_lwt_unix

  let url = "https://aireiter.com/api/openapi/submit"

  let payload = {|{
    "model": "grok_imagine_image_2_0",
    "params": {
      "prompt": "雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影",
      "aspect_ratio": "16:9",
      "resolution": "2K",
      "quality": "medium",
      "output_n": 1
    },
    "out_task_id": "grok_image_2_text_001"
  }|}

  let () =
    let headers = Header.init ()
      |> fun h -> Header.add h "Authorization" "Bearer <token>"
      |> fun h -> Header.add h "Content-Type" "application/json"
    in
    let body = Cohttp_lwt.Body.of_string payload in

    let response = Client.post ~headers ~body (Uri.of_string url) >>= fun (resp, body) ->
      body |> Cohttp_lwt.Body.to_string >|= fun body_str ->
      print_endline body_str
    in
    Lwt_main.run response
  ```

  ```dart Dart
  import 'dart:convert';
  import 'package:http/http.dart' as http;

  void main() async {
    final url = Uri.parse('https://aireiter.com/api/openapi/submit');

    final payload = {
          "model": "grok_imagine_image_2_0",
          "params": {
              "prompt": "雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影",
              "aspect_ratio": "16:9",
              "resolution": "2K",
              "quality": "medium",
              "output_n": 1
          },
          "out_task_id": "grok_image_2_text_001"
      };

    final response = await http.post(
      url,
      headers: {
        'Authorization': 'Bearer <token>',
        'Content-Type': 'application/json'
      },
      body: jsonEncode(payload),
    );

    print(response.body);
  }
  ```

  ```r R
  library(httr)
  library(jsonlite)

  url <- "https://aireiter.com/api/openapi/submit"

  payload <- list(
    model = "grok_imagine_image_2_0",
    params = list(
      prompt = "雨の夜の東京の街角、ネオンが濡れた路面に反射する、シネマティックな撮影",
      aspect_ratio = "16:9",
      resolution = "2K",
      quality = "medium",
      output_n = 1
    ),
    out_task_id = "grok_image_2_text_001"
  )

  response <- POST(
    url,
    add_headers(
      Authorization = "Bearer <token>",
      `Content-Type` = "application/json"
    ),
    body = toJSON(payload, auto_unbox = TRUE),
    encode = "raw"
  )

  cat(content(response, "text"))
  ```
</RequestExample>

<ResponseExample>
  ```json 200
{
    "statusCode": 200,
    "message": "",
    "data": {
        "out_task_id": "grok_image_2_text_001",
        "status": "pending",
        "estimated_credits": 100,
        "created_at": "2025-12-22T06:03:28.242Z"
    }
}
  ```

  ```json 400
    {
        "statusCode": 400,
        "message": "Request parameters are invalid",
        "ok": false
    }
  ```
  ```json 401
  {
    "error": {
      "code": 401,
      "message": "Authentication failed. Please check your API key."
    }
  }
  ```
```json 433
  {
    "error": {
      "code": 433,
      "message": "Insufficient account balance. Please top up and try again."
    }
  }
  ```

  ```json 500
  {
    "error": {
      "code": 500,
      "message": "Internal server error. Please try again later."
    }
  }
  ```
</ResponseExample>