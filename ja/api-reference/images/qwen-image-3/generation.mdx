---
title: 'Qwen Image 3.0 画像生成と編集'
description: |
    - 非同期処理モードで、後続のクエリ用にタスク ID を返します
    - テキストから画像生成と画像から画像生成をサポートし、参考画像を指定しない場合はテキストから画像生成になります
    - 最大 3 枚の参考画像と、1回の生成で 1-6 枚の画像をサポートします
    - 1K / 2K の解像度と 7 種類の一般的な画像比率をサポートします
    - 提出された prompt はプラットフォームの敏感語 / 安全審査を通過し、規約違反の内容は直接拒否されます
api: 'POST https://aireiter.com/api/openapi/submit'
---
import { apiKeyUrl } from '/snippets/_variables.mdx';

## Authorizations

<ParamField header="Authorization" type="string" required>
  すべてのエンドポイントは Bearer Token を使用して認証する必要があります。

  <a href={apiKeyUrl} target="_blank">API Key 管理ページ</a> にアクセスして API Key を取得し、Request Headers に次を追加してください:

  ```text
  Authorization: Bearer YOUR_API_KEY
  ```
</ParamField>

## Body

<ParamField body="model" type="string" required>
  モデル名、固定値: `"qwen_image_3"`。
</ParamField>

<ParamField body="params" type="object" required>
  モデルパラメータオブジェクト。

  <Expandable title="params オブジェクトの Properties">
    <ParamField body="prompt" type="string" required>
      画像生成または編集の指示。中国語と英語に対応しています。

      主体、シーン、構図、光、素材、テキスト内容、および参考画像で保持または変更が必要な部分を明確に記述することを推奨します。
    </ParamField>

    <ParamField body="image_url" type="string[]">
      参考画像の配列。送信すると画像から画像へのモードを使用します。送信しない場合はテキストから画像へのモードを使用します。

      * 最大 3 枚の参考画像
      * 1 枚あたりの画像サイズは最大 10MB
      * jpeg、png、webp をサポート
      * 公開アクセス可能な URL または完全な Base64 Data URI をサポート

      <Warning>
        画像 URL はインターネットから直接アクセス可能である必要があり、ログイン状態、Cookie、またはページの一時的な認証に依存してはいけません。
      </Warning>
    </ParamField>

    <ParamField body="resolution" type="string" default="1K">
      出力解像度のレベル。

      * `1K` - 基本解像度（デフォルト）
      * `2K` - 高解像度
    </ParamField>

    <ParamField body="aspect_ratio" type="string" default="1:1">
      出力画像のアスペクト比。

      以下の値をサポートしています:

      * `1:1` - 正方形（デフォルト）
      * `3:2` / `2:3` - 代表的な横長 / 縦長
      * `4:3` / `3:4` - 標準的な横長 / 縦長
      * `16:9` / `9:16` - ワイドスクリーンの横長 / 縦長

    </ParamField>

    <ParamField body="output_n" type="integer" default="1">
      このタスクで生成される画像数。値の範囲は `1` から `6` です。
    </ParamField>

    <ParamField body="prompt_extend" type="boolean" default="false">
      プロンプトのインテリジェントな書き換え。結果の予測可能性を確保するため、デフォルトでは無効です。インテリジェントな書き換えが必要な場合は `true` に設定してください。
    </ParamField>

    <ParamField body="prompt_extend_mode" type="string" default="direct">
      プロンプトの書き換え方式。`prompt_extend` が `true` の場合にのみ有効です。

      * `direct` - バランスの取れた書き換え。テキストから画像への生成と画像から画像への生成の両方をサポート
      * `agent` - 積極的な書き換え。テキストから画像への生成のみサポート
    </ParamField>

    <ParamField body="negative_prompt" type="string">
      ネガティブプロンプト。ぼかし、変形、誤った文字など、結果に含めたくない内容を記述するために使用します。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="out_task_id" type="string" required>
  呼び出し元が指定するタスク ID。冪等な送信と結果の取得に使用します。

  * 長さは 1〜64 文字
  * 英字、数字、アンダースコア、ハイフンのみを使用することを推奨します
  * 同じ API Key では重複して使用しないでください
</ParamField>

## Response

<ResponseField name="out_task_id" type="string">
  呼び出し側のタスク ID で、タスクの状態と結果をクエリするために使用できます。
</ResponseField>

<ResponseField name="status" type="string">
  初期状態で、通常は `"pending"` です。
</ResponseField>

<ResponseField name="created_at" type="string">
  タスク作成時刻、ISO 8601 形式。
</ResponseField>

## クエリ結果

送信が成功した後、同じ `out_task_id` を使用してクエリ API を呼び出します:

```bash
curl --request POST \
  --url https://aireiter.com/api/openapi/query \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "out_task_id": "qwen_image_3_task_001"
  }'
```

タスクのステータスには `pending`、`processing`、`completed`、`failed` が含まれます。タスク完了後、画像配列は `data.output` にあります。

<RequestExample>
  ```bash cURL（Text-to-Image）
  curl --request POST \
    --url https://aireiter.com/api/openapi/submit \
    --header 'Authorization: Bearer <token>' \
    --header 'Content-Type: application/json' \
    --data '{
      "model": "qwen_image_3",
      "params": {
        "prompt": "現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし",
        "resolution": "2K",
        "aspect_ratio": "16:9",
        "output_n": 1,
        "prompt_extend": true,
        "prompt_extend_mode": "agent"
      },
      "out_task_id": "qwen_image_3_task_001"
    }'
  ```

  ```bash cURL（Image-to-Image）
  curl --request POST \
    --url https://aireiter.com/api/openapi/submit \
    --header 'Authorization: Bearer <token>' \
    --header 'Content-Type: application/json' \
    --data '{
      "model": "qwen_image_3",
      "params": {
        "prompt": "商品の主体とラベル構造を維持し、背景をダークグレーの高級広告撮影スタジオに変更し、寒色系の輪郭光を追加する",
        "image_url": ["https://example.com/product-reference.png"],
        "resolution": "1K",
        "aspect_ratio": "1:1",
        "output_n": 1
      },
      "out_task_id": "qwen_image_3_edit_001"
    }'
  ```

  ```python Python
  import requests

  url = "https://aireiter.com/api/openapi/submit"

  payload = {
      "model": "qwen_image_3",
      "params": {
          "prompt": "現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし",
          "resolution": "2K",
          "aspect_ratio": "16:9",
          "output_n": 1,
          "prompt_extend": True,
          "prompt_extend_mode": "agent"
      },
      "out_task_id": "qwen_image_3_task_001"
  }

  headers = {
      "Authorization": "Bearer <token>",
      "Content-Type": "application/json"
  }

  response = requests.post(url, json=payload, headers=headers)

  print(response.json())
  ```

  ```javascript JavaScript
  const url = "https://aireiter.com/api/openapi/submit";

  const payload = {
    model: "qwen_image_3",
    params: {
      prompt: "現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし",
      resolution: "2K",
      aspect_ratio: "16:9",
      output_n: 1,
      prompt_extend: true,
      prompt_extend_mode: "agent"
    },
    out_task_id: "qwen_image_3_task_001"
  };

  const headers = {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  };

  fetch(url, {
    method: "POST",
    headers: headers,
    body: JSON.stringify(payload)
  })
    .then(response => response.json())
    .then(data => console.log(data))
    .catch(error => console.error('Error:', error));
  ```

  ```go Go
  package main

  import (
      "bytes"
      "encoding/json"
      "fmt"
      "io/ioutil"
      "net/http"
  )

  func main() {
      url := "https://aireiter.com/api/openapi/submit"

      payload := map[string]interface{}{
          "model": "qwen_image_3",
          "params": map[string]interface{}{
              "prompt": "現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし",
              "resolution": "2K",
              "aspect_ratio": "16:9",
              "output_n": 1,
              "prompt_extend": true,
              "prompt_extend_mode": "agent",
          },
          "out_task_id": "qwen_image_3_task_001",
      }

      jsonData, _ := json.Marshal(payload)

      req, _ := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
      req.Header.Set("Authorization", "Bearer <token>")
      req.Header.Set("Content-Type", "application/json")

      client := &http.Client{}
      resp, err := client.Do(req)
      if err != nil {
          panic(err)
      }
      defer resp.Body.Close()

      body, _ := ioutil.ReadAll(resp.Body)
      fmt.Println(string(body))
  }
  ```

  ```java Java
  import java.net.http.HttpClient;
  import java.net.http.HttpRequest;
  import java.net.http.HttpResponse;
  import java.net.URI;

  public class Main {
      public static void main(String[] args) throws Exception {
          String url = "https://aireiter.com/api/openapi/submit";

          String payload = """
              {
                "model": "qwen_image_3",
                "params": {
                  "prompt": "現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし",
                  "resolution": "2K",
                  "aspect_ratio": "16:9",
                  "output_n": 1,
                  "prompt_extend": true,
                  "prompt_extend_mode": "agent"
                },
                "out_task_id": "qwen_image_3_task_001"
              }
          """;

          HttpClient client = HttpClient.newHttpClient();
          HttpRequest request = HttpRequest.newBuilder()
              .uri(URI.create(url))
              .header("Authorization", "Bearer <token>")
              .header("Content-Type", "application/json")
              .POST(HttpRequest.BodyPublishers.ofString(payload))
              .build();

          HttpResponse<String> response = client.send(request,
              HttpResponse.BodyHandlers.ofString());

          System.out.println(response.body());
      }
  }
  ```

  ```php PHP
  <?php

  $url = "https://aireiter.com/api/openapi/submit";

  $payload = [
      "model" => "qwen_image_3",
      "params" => [
          "prompt" => "現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし",
          "resolution" => "2K",
          "aspect_ratio" => "16:9",
          "output_n" => 1,
          "prompt_extend" => true,
          "prompt_extend_mode" => "agent"
      ],
      "out_task_id" => "qwen_image_3_task_001"
  ];

  $ch = curl_init($url);
  curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
  curl_setopt($ch, CURLOPT_POST, true);
  curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($payload));
  curl_setopt($ch, CURLOPT_HTTPHEADER, [
      "Authorization: Bearer <token>",
      "Content-Type: application/json"
  ]);

  $response = curl_exec($ch);
  curl_close($ch);

  echo $response;
  ?>
  ```

  ```ruby Ruby
  require 'net/http'
  require 'json'
  require 'uri'

  url = URI("https://aireiter.com/api/openapi/submit")

  payload = {
    model: "qwen_image_3",
    params: {
      prompt: "現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし",
      resolution: "2K",
      aspect_ratio: "16:9",
      output_n: 1,
      prompt_extend: true,
      prompt_extend_mode: "agent"
    },
    out_task_id: "qwen_image_3_task_001"
  }

  http = Net::HTTP.new(url.host, url.port)

  request = Net::HTTP::Post.new(url)
  request["Authorization"] = "Bearer <token>"
  request["Content-Type"] = "application/json"
  request.body = payload.to_json

  response = http.request(request)
  puts response.body
  ```

  ```swift Swift
  import Foundation

  let url = URL(string: "https://aireiter.com/api/openapi/submit")!

  let payload: [String: Any] = [
      "model": "qwen_image_3",
      "params": [
          "prompt": "現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし",
          "resolution": "2K",
          "aspect_ratio": "16:9",
          "output_n": 1,
          "prompt_extend": true,
          "prompt_extend_mode": "agent"
      ],
      "out_task_id": "qwen_image_3_task_001"
  ]

  var request = URLRequest(url: url)
  request.httpMethod = "POST"
  request.setValue("Bearer <token>", forHTTPHeaderField: "Authorization")
  request.setValue("application/json", forHTTPHeaderField: "Content-Type")
  request.httpBody = try? JSONSerialization.data(withJSONObject: payload)

  let task = URLSession.shared.dataTask(with: request) { data, response, error in
      if let error = error {
          print("Error: \(error)")
          return
      }

      if let data = data, let responseString = String(data: data, encoding: .utf8) {
          print(responseString)
      }
  }

  task.resume()
  ```

  ```csharp C#
  using System;
  using System.Net.Http;
  using System.Text;
  using System.Threading.Tasks;

  class Program
  {
      static async Task Main(string[] args)
      {
          var url = "https://aireiter.com/api/openapi/submit";

          var payload = @"
                {
                    ""model"": ""qwen_image_3"",
                    ""params"": {
                        ""prompt"": ""現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし"",
                        ""resolution"": ""2K"",
                        ""aspect_ratio"": ""16:9"",
                        ""output_n"": 1,
                        ""prompt_extend"": true,
                        ""prompt_extend_mode"": ""agent""
                    },
                    ""out_task_id"": ""qwen_image_3_task_001""
                }";

          using var client = new HttpClient();
          client.DefaultRequestHeaders.Add("Authorization", "Bearer <token>");

          var content = new StringContent(payload, Encoding.UTF8, "application/json");
          var response = await client.PostAsync(url, content);
          var result = await response.Content.ReadAsStringAsync();

          Console.WriteLine(result);
      }
  }
  ```

  ```c C
  #include <stdio.h>
  #include <curl/curl.h>

  int main(void) {
      CURL *curl;
      CURLcode res;

      curl_global_init(CURL_GLOBAL_DEFAULT);
      curl = curl_easy_init();

      if(curl) {
          const char *url = "https://aireiter.com/api/openapi/submit";
          const char *payload = "{\"model\":\"qwen_image_3\",\"params\":{\"prompt\":\"現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし\",\"resolution\":\"2K\",\"aspect_ratio\":\"16:9\",\"output_n\":1,\"prompt_extend\":true,\"prompt_extend_mode\":\"agent\"},\"out_task_id\":\"qwen_image_3_task_001\"}";

          struct curl_slist *headers = NULL;
          headers = curl_slist_append(headers, "Authorization: Bearer <token>");
          headers = curl_slist_append(headers, "Content-Type: application/json");

          curl_easy_setopt(curl, CURLOPT_URL, url);
          curl_easy_setopt(curl, CURLOPT_POSTFIELDS, payload);
          curl_easy_setopt(curl, CURLOPT_HTTPHEADER, headers);

          res = curl_easy_perform(curl);

          if(res != CURLE_OK) {
              fprintf(stderr, "curl_easy_perform() failed: %s\n",
                      curl_easy_strerror(res));
          }

          curl_slist_free_all(headers);
          curl_easy_cleanup(curl);
      }

      curl_global_cleanup();
      return 0;
  }
  ```

  ```objectivec Objective-C
  #import <Foundation/Foundation.h>

  int main(int argc, const char * argv[]) {
      @autoreleasepool {
          NSURL *url = [NSURL URLWithString:@"https://aireiter.com/api/openapi/submit"];

          NSDictionary *payload = @{
              @"model": @"qwen_image_3",
              @"params": @{
                  @"prompt": @"現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし",
                  @"resolution": @"2K",
                  @"aspect_ratio": @"16:9",
                  @"output_n": @1,
                  @"prompt_extend": @YES,
                  @"prompt_extend_mode": @"agent"
              },
              @"out_task_id": @"qwen_image_3_task_001"
          };

          NSError *error;
          NSData *jsonData = [NSJSONSerialization dataWithJSONObject:payload
                                                            options:0
                                                              error:&error];

          NSMutableURLRequest *request = [NSMutableURLRequest requestWithURL:url];
          [request setHTTPMethod:@"POST"];
          [request setValue:@"Bearer <token>" forHTTPHeaderField:@"Authorization"];
          [request setValue:@"application/json" forHTTPHeaderField:@"Content-Type"];
          [request setHTTPBody:jsonData];

          NSURLSessionDataTask *task = [[NSURLSession sharedSession]
              dataTaskWithRequest:request
              completionHandler:^(NSData *data, NSURLResponse *response, NSError *error) {
                  if (error) {
                      NSLog(@"Error: %@", error);
                      return;
                  }
                  NSString *result = [[NSString alloc] initWithData:data
                                                          encoding:NSUTF8StringEncoding];
                  NSLog(@"%@", result);
              }];

          [task resume];
          [[NSRunLoop mainRunLoop] run];
      }
      return 0;
  }
  ```

  ```ocaml OCaml
  (* Requires cohttp and yojson libraries *)
  open Lwt
  open Cohttp
  open Cohttp_lwt_unix

  let url = "https://aireiter.com/api/openapi/submit"

  let payload = {|{
    "model": "qwen_image_3",
    "params": {
      "prompt": "現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし",
      "resolution": "2K",
      "aspect_ratio": "16:9",
      "output_n": 1,
      "prompt_extend": true,
      "prompt_extend_mode": "agent"
    },
    "out_task_id": "qwen_image_3_task_001"
  }|}

  let () =
    let headers = Header.init ()
      |> fun h -> Header.add h "Authorization" "Bearer <token>"
      |> fun h -> Header.add h "Content-Type" "application/json"
    in
    let body = Cohttp_lwt.Body.of_string payload in

    let response = Client.post ~headers ~body (Uri.of_string url) >>= fun (resp, body) ->
      body |> Cohttp_lwt.Body.to_string >|= fun body_str ->
      print_endline body_str
    in
    Lwt_main.run response
  ```

  ```dart Dart
  import 'dart:convert';
  import 'package:http/http.dart' as http;

  void main() async {
    final url = Uri.parse('https://aireiter.com/api/openapi/submit');

    final payload = {
          "model": "qwen_image_3",
          "params": {
              "prompt": "現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし",
              "resolution": "2K",
              "aspect_ratio": "16:9",
              "output_n": 1,
              "prompt_extend": true,
              "prompt_extend_mode": "agent"
          },
          "out_task_id": "qwen_image_3_task_001"
      };

    final response = await http.post(
      url,
      headers: {
        'Authorization': 'Bearer <token>',
        'Content-Type': 'application/json'
      },
      body: jsonEncode(payload),
    );

    print(response.body);
  }
  ```

  ```r R
  library(httr)
  library(jsonlite)

  url <- "https://aireiter.com/api/openapi/submit"

  payload <- list(
    model = "qwen_image_3",
    params = list(
      prompt = "現代的なガラス建築の前にある未来感のある電気スポーツカー、ブルーアワー、商業自動車写真、リアルな質感と自然な影、文字なし、透かしなし",
      resolution = "2K",
      aspect_ratio = "16:9",
      output_n = 1,
      prompt_extend = TRUE,
      prompt_extend_mode = "agent"
    ),
    out_task_id = "qwen_image_3_task_001"
  )

  response <- POST(
    url,
    add_headers(
      Authorization = "Bearer <token>",
      `Content-Type` = "application/json"
    ),
    body = toJSON(payload, auto_unbox = TRUE),
    encode = "raw"
  )

  cat(content(response, "text"))
  ```
</RequestExample>

<ResponseExample>
  ```json 200
{
    "statusCode": 200,
    "message": "",
    "data": {
        "out_task_id": "qwen_image_3_task_001",
        "status": "pending",
        "estimated_credits": 100,
        "created_at": "2025-12-22T06:03:28.242Z"
    }
}
  ```

  ```json 400
    {
        "statusCode": 400,
        "message": "リクエストパラメータが無効です",
        "ok": false
    }
  ```
  ```json 401
  {
    "error": {
      "code": 401,
      "message": "認証に失敗しました。APIキーを確認してください"
    }
  }
  ```
```json 433
  {
    "error": {
      "code": 433,
      "message": "アカウント残高が不足しています。チャージしてから再試行してください"
    }
  }
  ```

  ```json 500
  {
    "error": {
      "code": 500,
      "message": "サーバー内部エラーです。しばらくしてから再試行してください"
    }
  }
  ```
</ResponseExample>